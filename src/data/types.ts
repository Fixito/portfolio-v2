export type SectionMeta = {
  title: string;
  description: string;
};

export type SocialLink = {
  url: string;
  icon: string;
  title: string;
};

export type NavLink = {
  href: string;
  text: string;
};
